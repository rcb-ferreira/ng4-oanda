import { TechanDirective } from './techan.directive';

describe('TechanDirective', () => {
  it('should create an instance', () => {
    const directive = new TechanDirective();
    expect(directive).toBeTruthy();
  });
});
